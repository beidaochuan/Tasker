# Local development and preview configuration.
# Copy this file to .env and replace both placeholder values.
# Never commit .env because it contains real credentials.
TASKER_ADMIN_USERNAME=replace-with-your-admin-name
TASKER_ADMIN_PASSWORD=replace-with-a-strong-password-of-at-least-12-characters
